*.js
!jest.config.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# This repo is public. cdk.context.json holds the account id, VPC / subnet /
# security-group ids, KMS ARN and the resolved subnet layout — a map of the
# AWS environment. We keep it (and any local context file) OUT of git and
# supply db:* / env:* context locally instead. See README.
cdk.context.json
cdk.context.local.json
